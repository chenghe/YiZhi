<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v4.view.ViewPager
        android:id="@+id/id_guide_viewpager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></android.support.v4.view.ViewPager>

    <com.rd.PageIndicatorView
        android:id="@+id/id_guide_pageIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        app:piv_animationType="worm"
        android:layout_marginBottom="24dp"
        />

    <TextView
        android:id="@+id/id_guide_start_home"
        android:visibility="gone"
        android:layout_width="115dp"
        android:layout_height="38dp"
        android:layout_alignParentBottom="true"
        android:paddingRight="20dp"
        android:paddingLeft="20dp"
        android:paddingBottom="8dp"
        android:paddingTop="8dp"
        android:gravity="center"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/id_guide_pageIndicator"
        android:layout_margin="48dp"
        android:textSize="16sp"
        android:background="@drawable/select_btn_edge_color_green"
        android:textColor="@color/guaid_btn"
        android:text="立即体验"/>
</RelativeLayout>
